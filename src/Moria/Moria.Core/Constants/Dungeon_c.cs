namespace Moria.Core.Constants
{
    public static class Dungeon_c
    {
        public const uint RATIO = 3;       // Size ratio of the Map screen
        public const uint MAX_HEIGHT = 66; // Multiple of 11; >= 22
        public const uint MAX_WIDTH = 198; // Multiple of 33; >= 66
        public const uint SCREEN_HEIGHT = 22;
        public const uint SCREEN_WIDTH = 66;
        public const uint QUART_HEIGHT = SCREEN_HEIGHT / 4;
        public const uint QUART_WIDTH = SCREEN_WIDTH / 4;
    }
}
